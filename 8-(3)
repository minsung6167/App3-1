문제 30. 클로드 코드에서 API 키를 안전하게 관리하기 위한 방법이 아닌 것을 고르세요.
   ① .env 파일에 API 키 저장
   ② .gitignore 파일에 .env 추가
   ③ 코드에 직접 API 키 하드코딩
   ④ 프로젝트 폴더 내에서만 키 참조
답 : 3

문제 31. 오픈라우터의 Activity 메뉴에서 확인할 수 있는 정보가 아닌 것은?
   ① 사용한 모델명
   ② 사용 시간
   ③ 호출한 코드 내용
   ④ API 호출 기록
답 : 3

문제 32. PRD를 3단계로 분리하여 개발할 때, 2단계에서 텍스트 모델(Qwen 등) API 호출이 실패
하면 다음 중 점검이 필요한 항목을 모두 고르세요.
   ① .env 파일의 OPENROUTER_API_KEY 존재 여부
   ② 텍스트 모델명의 정확성
   ③ 1단계에서 Streamlit 서버가 이미 8501 포트를 사용 중인지 여부
   ④ .gitignore 파일의 존재 여부
답 : 1, 2

문제 33. Streamlit과 전통적인 웹 개발(HTML/CSS/JS)의 차이점으로 틀린 것을 고르세요.
   ① Streamlit은 파이썬 코드만으로 웹 앱을 만들 수 있다.
   ② Streamlit은 데이터 시각화와 ML 모델 시연에 특화되어 있다.
   ③ Streamlit은 반드시 프런트엔드와 백엔드를 분리해야 한다.
   ④ Streamlit은 streamlit run 명령어로 실행 가능하다.
답 : 3

문제 34. 커스텀 명령어와 에이전트의 기술적 차이점으로 옳은 것을 고르세요.
   ① 커스텀 명령어는 .md 파일로 저장되지만 에이전트는 .json 파일로 저장된다.
   ② 에이전트는 독립적인 AI 모델을 선택할 수 있지만 커스텀 명령어는 메인 세션의 모델을 공유
       한다.
   ③ 커스텀 명령어가 에이전트보다 더 복잡한 작업 플로우를 처리할 수 있다.
   ④ 에이전트는 적용을 위해 재시작이 필요하지만 커스텀 명령어는 바로 사용할 수 있다.
답 : 2

문제 35. 에이전트 생성 시 '4. Inherit from parent' 옵션을 선택했을 때의 동작으로 옳은 것을 고르세요.
   ① 항상 가장 저렴한 모델을 자동 선택한다.
   ② 메인 세션에서 사용 중인 모델을 그대로 상속받는다.
   ③ 부모 에이전트가 사용하는 모델을 복사한다.
   ④ Claude의 최신 모델로 자동 업그레이드된다.
답 : 2

문제 36. 에이전트에게 Read-only 권한만 부여했을 때 수행할 수 없는 작업을 모두 고르세요.
   ① 코드 파일 내용 읽기 및 분석    ② 버그 리포트 생성
   ③ 발견한 버그 수정                   ④ 새로운 테스트 파일 생성
답 : 3,4

문제 37. AI 공감 다이어리 개발에서 백엔드 개발과 프런트엔드 개발보다 품질 보증(QA) 테스트 시간이
더 오래 걸린 이유로 가장 적절한 것을 고르세요.
   ① QA 에이전트가 더 느린 AI 모델을 사용하기 때문이다.
   ② QA 에이전트는 이전 에이전트들의 작업 결과를 모두 검토해야 하기 때문이다.
   ③ QA 에이전트만 인터넷 연결이 필요하기 때문이다.
   ④ QA 에이전트는 파일 읽기 권한이 없기 때문이다.
답 : 1

문제 38. PDF 요약 도구에서 제품 기획 관리자 에이전트가 생성한 PRD를 다른 에이전트들이 참조하는
방식으로 옳지 않은 것을 고르세요.
   ① PRD가 프로젝트 폴더에 생성되어 각 에이전트들이 접근한다.
   ② 에이전트들이 순차적, 혹은 병렬로 작업하며 PRD를 참조하게 된다.
   ③ PRD 파일 경로를 에이전트별로 지정하지 않아도 된다.
   ④ PRD는 '.claude/agents/' 폴더에 자동으로 복사되어 공유된다.
답 : 4

문제 39. MCP와 API의 차이점으로 가장 적절한 설명을 고르세요.
   ① API는 특정 기능을 요청하는 방식이고, MCP는 AI가 외부 시스템과 직접 소통하게 하는 프로
       토콜
   ② MCP는 API보다 속도가 빠르다
   ③ API는 유료이고 MCP는 무료이다
   ④ MCP는 클로드 코드에서만 사용 가능하다
답 : 2

문제 40. 로컬 MCP 서버 설치 명령어에서 -- 구분자의 역할로 옳은 것을 고르세요.
   ① 주석을 표시하는 기호
   ② 클로드 코드 옵션과 MCP 서버 실행 명령을 구분
   ③ 버전 정보를 나타내는 표시
   ④ 에러 처리를 위한 구분자
답 : 2

문제 41. Playwright MCP가 테스트 완료 후 생성하는 폴더 이름을 고르세요.
   ① .test-results ② .playwright-mcp ③ .automation ④ .screenshots
답 : 2

문제 42. 깃허브에 코드를 업로드하는 순서로 올바른 것을 고르세요.
   ① commit → add → push   ② push → add → commit
   ③ add → commit → push   ④ add → push → commit
답 : 3

문제 43. 로컬 스토리지와 클라우드 데이터베이스의 가장 큰 차이점은?
   ① 로컬 스토리지가 더 빠르다.
   ② 데이터베이스는 여러 기기에서 데이터를 공유할 수 있다.
   ③ 로컬 스토리지가 더 안전하다.
   ④ 데이터베이스는 무료가 없다.
답 : 2

44. Vercel 배포 시 404 에러가 발생한 이유와 해결 방법으로 옳은 것을 고르세요.
   ① 서버 오류 – 재시작 필요
   ② index.html 파일 부재 – 파일명 변경 필요
   ③ 인증 실패 – 재로그인 필요
   ④ 네트워크 오류 – 연결 확인 필요
답 : 2
